"""gov-doc-collector 采集器脚本包。

支持两种使用方式:
    # 1) 仓库根目录作为包导入(README 用法)
    from scripts.unified_fetcher import UnifiedFetcher

    # 2) 直接运行脚本(python scripts/xxx.py)
    #    模块内部有 try/except 相对导入回退,两种方式均可
"""
